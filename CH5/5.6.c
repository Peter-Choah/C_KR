/* Rewrite appropriate programs from earlier chapters and exercises
 * with pointers instead of array indexing. E.g. getline (CH1,CH4),
 * atoi, itoa, and variants (CH2, CH3, CH4), reverse (CH3), and
 * strindex and getop (CH4)*/
